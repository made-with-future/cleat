version: 1
docker: true
services:
  - name: backend
    dir: .
    modules:
      - python:
          django: true
          django_service: backend
          package_manager: uv
      - npm:
          service: backend-node
          scripts:
            - build
