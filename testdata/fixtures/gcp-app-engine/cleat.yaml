version: 1
google_cloud_platform:
  project_name: my-gcp-project
  account: user@example.com
services:
  - name: default
    dir: .
    modules:
      - python:
          django: true
          package_manager: pip
  - name: backend
    dir: ./backend
    modules:
      - python:
          django: false
